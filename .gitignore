# ── Credenciais / segredos ──
.env
.env.*
!.env.livesync.example

# ── Binários Go ──
*.exe
*.exe~
*.dll
*.so
*.dylib
/worker

# ── Testes ──
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# ── Go workspace ──
go.work
go.work.sum

# ── IDEs / Editores ──
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Docker / runtime ──
tmp/

# ── Dados locais de debug (JSON de leitura bruta) ──
leitura.json
